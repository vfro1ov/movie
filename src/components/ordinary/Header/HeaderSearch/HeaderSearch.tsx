import { FunctionComponent } from 'react';

export const HeaderSearch: FunctionComponent<HeaderSearchProps> = (props) => {
	const {} = props;

	return (
		<>
			<input type="text" />
		</>
	);
};

export interface HeaderSearchProps {}
